const CANVAS_WIDTH = 1280;
const CANVAS_HEIGHT = 960;
const NUM_COLUMNS = Math.floor(CANVAS_WIDTH/16);
const NUM_ROWS = parseInt(CANVAS_HEIGHT/16);
const PIXELS_PER_UNIT = 16;

export {CANVAS_HEIGHT,CANVAS_WIDTH,NUM_COLUMNS,NUM_ROWS,PIXELS_PER_UNIT}